extends layout

block content
  a(href="/") Back to home
  h3.
    Note Details (#{data.name})
  h4.
    Date Created
  span #{moment(data.created).format("YYYY-MM-DD")}
  h4.
    Note Contents
  span #{data.contents}
  br
